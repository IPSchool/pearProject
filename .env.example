# Dev proxy target (pearProjectApi docker/jira)
VITE_API_PROXY_TARGET=http://127.0.0.1:8090

# Production: full API base including trailing path segment if needed
# VITE_API_URL=https://your-api.example.com

VITE_APP_TITLE=PearProject

# Optional WebSocket (GatewayWorker). Leave empty to disable realtime badge.
# VITE_WS_URL=ws://127.0.0.1:8282
